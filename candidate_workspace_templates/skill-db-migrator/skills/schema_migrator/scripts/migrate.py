def audit_sql_query(query):
    # Starter code
    # Candidates/Agents must inspect query strings for standard index builds
    # and rewrite them to run CONCURRENTLY to prevent locking transactions.
    return query

def generate_rollback_sql(query):
    # Candidates/Agents must generate non-blocking index drop actions.
    return ""
