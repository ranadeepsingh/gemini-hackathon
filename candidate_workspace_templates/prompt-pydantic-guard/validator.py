def is_conforming_json(response):
    # Buggy starter code: returns False always.
    # Candidates/Agents must add standard JSON decoding checks
    # and confirm that required schema keys 'name' and 'email' are populated.
    return False
