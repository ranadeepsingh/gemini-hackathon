def sanitize_pii(text):
    # Starter code: returns raw input without redaction.
    # Candidates/Agents must add regex replace operations to filter SSNs (000-00-0000)
    # and phone numbers (000-000-0000).
    return text
